# Tasks: Persistência local (SQLite)

Referência: `spec.md` + `plan.md` nesta mesma pasta. Ordem importa — de cima pra baixo.

- [x] T1 — `pubspec.yaml`: adiciona `sqflite` (`dependencies`) e `sqflite_common_ffi` (`dev_dependencies`). `flutter pub get`.
- [x] T2 — `lib/data/services/app_database.dart` — `AppDatabase`, abre/gerencia o banco (`toys`/`rentals`, `onCreate`), `Future<Database> get database` cacheada.
- [x] T3 — `lib/data/services/toy_local_service.dart` — `loadAll()`/`upsert()`/`delete()`, mapeamento `Toy`↔linha (ver tabela em `plan.md`).
- [x] T4 — `lib/data/services/rental_local_service.dart` — `loadAll()`/`upsert()`/`delete()`, mapeamento `Rental`↔linha.
- [x] T5 — `test/data/app_database_test.dart`, `toy_local_service_test.dart`, `rental_local_service_test.dart` — com `sqflite_common_ffi`, cobrindo CRUD real de cada Service (não mock). 11 testes, todos verdes.
- [x] T6 — `lib/data/repositories/toy_repository.dart` — injeta `ToyLocalService`, `load()` (hidrata; semeia `kInitialToys` só se tabela vazia), mutações existentes persistem via `unawaited` + `try/catch`.
- [x] T7 — `lib/data/repositories/rental_repository.dart` — injeta `RentalLocalService`; construtor default passa a `rentals = []`; `_seedInitial()` só usado por `RentalRepository.withDemoSeed()` (construtor nomeado novo); `load()` (hidrata; sem seed de demonstração na primeira execução real); mutações existentes (`addNew`/`add`/`removeById`/`extend`/`finish`) persistem via `unawaited` + `try/catch`.
- [x] T8 — Cobertura de `load()`/seed pendente ficou dentro de T7 (`test/rental_repository_test.dart` ganhou o teste "the default constructor starts empty"); `load()` em si (hidratação real via `LocalService`) coberto pelos testes de `test/data/` (T5) — o Repository só delega pro Service, sem lógica nova a testar isoladamente.
- [x] T9 — Localizados por `grep` todos os call sites de `RentalRepository()`/`SonhoDeCriancaApp()` que dependiam implicitamente da seed de demonstração — trocados pra `RentalRepository.withDemoSeed()` (direto ou via `SonhoDeCriancaApp(rentalRepository: ...)`/`_pumpApp(tester, rentalRepository: ...)` novo parâmetro opcional). Arquivos tocados: `active_rentals_cubit_test.dart`, `report_cubit_test.dart`, `home_cubit_test.dart`, `catalog_tickets_test.dart`, `rental_repository_test.dart`, `toy_catalog_cubit_test.dart`, `design_v3_test.dart`, `pix_flow_test.dart`, `report_view_test.dart`, `full_app_journey_test.dart`. Nenhum assert de comportamento mudou (`rental_repository_test.dart` ganhou 1 teste novo pro comportamento novo — default vazio — sem remover a cobertura da seed, só reapontada pra `withDemoSeed()`).
- [x] T10 — `lib/state/app_state.dart`: remove a chamada a `_businessSettingsRepository.load()` do construtor.
- [x] T11 — `lib/main.dart`: `main()` assíncrono, `WidgetsFlutterBinding.ensureInitialized()`, `await Future.wait([...load()])` antes de `runApp`; `SonhoDeCriancaApp` ganha 3 parâmetros opcionais (default `null` → cada Repository cai no próprio construtor sem `_localService`, preservando `const SonhoDeCriancaApp()` pros ~10 arquivos de teste que pumpam a árvore real).
- [x] T12 — `specs/constitution.md`, seção "Segurança (baseline)": atualiza a frase sobre "sem persistência entre sessões".
- [x] T13 — `flutter analyze` limpo.
- [x] T14 — `flutter test` completo, sem alterar nenhum assert existente (só as trocas mecânicas de construtor da T9). 105 testes, todos verdes (93 anteriores + 11 novos de `test/data/` + 1 novo em `rental_repository_test.dart`).
- [x] T15 — Rodado de verdade num iPhone 16 (iOS Simulator, `flutter run`): boot limpo, sem crash, sem flicker perceptível — Painel do dia mostrou direto o estado real (R$ 0,00 / "Nenhuma atividade ainda hoje"), sem os dados fictícios antigos. Toque automatizado na simulator não foi possível neste ambiente (sem permissão de Acessibilidade concedida ao terminal pra `osascript`/`System Events`) — cenários 1 e 2 (brinquedo/locação sobrevivendo a fechar+reabrir) ficaram cobertos, de forma mais forte e permanente que um clique manual único, por `test/data/persistence_round_trip_test.dart` (novo): 2 "boots" reais contra o mesmo arquivo SQLite (via `sqflite_common_ffi`, não `inMemoryDatabasePath`), Repository/Service recriados do zero no segundo boot — prova literalmente o cenário do spec, incluindo um teste extra de não-regressão (segundo boot não re-semeia `kInitialToys` em cima do que já existe).
- [x] T16 — Revisão manual dos critérios de aceite do `spec.md` + `spec.md` Status vira `Implemented` + atualizar `specs/README.md` (linha 020).

Marcar cada task ao concluir. Ao final, `spec.md` Status vira `Implemented`.
